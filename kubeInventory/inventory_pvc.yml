apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: luris
  name: inventory-order-luris-api-final-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-client